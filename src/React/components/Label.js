import React from 'react';

function Label({label}) {
    return (
        <div className={"label-title"}>
            <h4>{label}</h4>
            <div></div>
        </div>
    );
}

export default Label;
